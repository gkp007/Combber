import React from 'react';
import {Box, Text} from '@gluestack-ui/themed';
const Router = () => {
  return (
    <Box flex={1} bg={'white'}>
      <Text bold size={'lg'}>
        Router
      </Text>
    </Box>
  );
};

export default Router;
